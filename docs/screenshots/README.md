# Screenshots

Placeholder directory for README screenshots. Once these exist, uncomment
the corresponding image links in the root `README.md`'s Frontend section.

- `home-light.png` — the New Idea / home page, light mode
- `workspace-light.png` — the Workspace page mid-pipeline (e.g. Tasks
  section with the Epic → User Story → Task accordion expanded), light mode
- `workspace-dark.png` — the same Workspace view, dark mode
- `mobile.png` — the Workspace page at a mobile viewport width
