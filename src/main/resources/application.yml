spring:
  application:
    name: blueforge

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${POSTGRES_DB:blueforge}
    username: ${POSTGRES_USER:blueforge}
    password: ${POSTGRES_PASSWORD:blueforge}

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true
